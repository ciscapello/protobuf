syntax = "proto3";

package auth;
option go_package = "github.com/ciscapello/protobuf/auth;authpb";

service AuthService {
	rpc Register (RegisterReguest) returns (RegisterResponse);
}

message RegisterReguest {
	string email = 1;
}

message RegisterResponse {
	string id = 1;
}
